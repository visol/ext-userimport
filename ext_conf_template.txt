# cat=modules//; type=string; label= Path to (protected) upload folder. : Example 'N:/user_upload/userimport/' where the id (N) has to match the uid of your (protected) storage record and the path is relative to the storage folder selected.
uploadStorageFolder =
